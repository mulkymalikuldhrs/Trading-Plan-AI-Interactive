name: flutter_app
description: Dhaher Trading Plan AI Flutter App
version: 10.5.5-hardened

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  rive: ^0.9.1
  speech_to_text: ^5.6.0
  fl_chart: ^0.55.0
  webview_flutter: ^3.0.4
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/animations/
