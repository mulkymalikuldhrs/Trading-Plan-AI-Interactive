name: flutter_app
description: Dhaher Trading Plan AI Flutter App
publish_to: 'none'
version: 10.5.8-hardened

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  rive: ^0.14.7
  speech_to_text: ^7.4.0
  fl_chart: ^1.2.0
  webview_flutter: ^4.13.1
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/rive/
